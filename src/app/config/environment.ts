/**
 * Configuration de l'environnement
 * En dev, les requêtes /api sont proxifiées par Vite vers le backend.
 * En production (Vercel), le rewrite est configuré dans vercel.json.
 */
export const API_BASE_URL = '/api';
